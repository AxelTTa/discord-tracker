[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = [
  "python3 -m venv .venv",
  ".venv/bin/pip install -r requirements.txt"
]

[start]
cmd = ".venv/bin/python tracker.py"

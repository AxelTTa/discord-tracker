[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/python bot.py"
healthcheckPath = "/internal/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
